# Generated.
pnpm-lock.yaml

# Markdown is hand-authored (spec, CLAUDE.md, ADRs). Prettier reflows tables and
# lists, which would rewrite documents this project treats as truth. Prettier's
# job here is code and config formatting.
*.md

# Playwright output. Prettier defaults its ignore path to .gitignore too, but
# the gate must stay green whatever order a developer runs things in.
playwright-report/
test-results/
