// Public API of the `hud` widget. `app` composes Hud; ScoreCounter, StatusBadge
// and the `statusBadgeVariant` precedence behind it are internals of the bar.
export type { HudProps } from './Hud';
export { Hud } from './Hud';
